<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.mousehole.americanairline.membersonly">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MembersOnly">
        <activity android:name=".activity.addmember.view.AddMemberActivity" />
        <activity android:name=".activity.changepassword.view.ChangePasswordActivity" />
        <activity android:name=".activity.memberdisplay.view.MemberDisplayActivity" />
        <activity android:name=".activity.listdisplay.view.MemberListActivity" />
        <activity android:name=".activity.login.view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>